<main class="container">
  <%= link_to sanitize("&larr; Back to #{@quote.name}"), quote_path(@quote) %>

  <div class="header">
    <h1>New date</h1>
  </div>
    <%= turbo_frame_tag @line_item_date do %>
      <%= render "date_form", quote: @quote, line_item_date: @line_item_date %>
    <% end %>
</main>
